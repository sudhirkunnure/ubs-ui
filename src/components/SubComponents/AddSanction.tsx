import { connect } from 'react-redux';
import React, { Component } from 'react';
import { Link } from 'react-router-dom';



const AddSanction = () => (
    <div>

Add Securities



   </div>
);

export default connect()(AddSanction);
